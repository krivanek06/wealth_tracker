fragment User on User {
	id
	createdAt
	imageUrl
	username
	email
	lastSingInDate
	accountType
	authentication {
		authenticationType
	}
}

fragment LoggedUserOutput on LoggedUserOutput {
	accessToken
}
